/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { JednostkiChoroboweService } from './JednostkiChorobowe.service';

describe('Service: JednostkiChorobowe', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [JednostkiChoroboweService]
    });
  });

  it('should ...', inject([JednostkiChoroboweService], (service: JednostkiChoroboweService) => {
    expect(service).toBeTruthy();
  }));
});
